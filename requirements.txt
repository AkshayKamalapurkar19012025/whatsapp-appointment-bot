# Web framework
fastapi==0.141.1
uvicorn[standard]==0.52.4

# Data validation (already an implicit dependency via fastapi/pydantic models)
pydantic==2.13.5

# PostgreSQL driver + connection pool
psycopg[binary]==3.3.5
psycopg-pool==3.3.1

# Loads DB_*/DATABASE_URL from a local .env file, if present, without
# overriding real environment variables that are already set.
python-dotenv==1.2.3

# Password hashing for staff/admin accounts (WEB P5). Argon2id is the
# current OWASP-recommended default for new applications; argon2-cffi is
# the actively maintained Python binding (unlike passlib, which is in
# maintenance mode).
argon2-cffi==23.1.0

# Doctor profile photo upload (multipart form parsing is a required peer
# dependency for FastAPI's UploadFile, not bundled by fastapi itself) and
# server-side image validation/normalization (real format/dimension
# checks via decoding, not trusting the client's claimed content-type;
# re-encoding strips EXIF and lets us downscale oversized uploads).
python-multipart==0.0.32
Pillow==12.3.0
